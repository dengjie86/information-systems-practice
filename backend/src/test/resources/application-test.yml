spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:dorm_repair;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE;NON_KEYWORDS=USER
    username: sa
    password:
  sql:
    init:
      mode: always
      schema-locations: classpath:schema-h2.sql

