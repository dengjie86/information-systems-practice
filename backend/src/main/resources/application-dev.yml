spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/dorm_repair?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: ${DORM_DB_USERNAME:root}
    password: ${DORM_DB_PASSWORD:123456}

logging:
  level:
    root: info
    com.dormrepair: debug
